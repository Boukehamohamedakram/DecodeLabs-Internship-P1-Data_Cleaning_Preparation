# Architecture

This document explains the folder structure, data pipeline, and architectural decisions for Project P1.

## Project Layout

- `config/` for configuration templates
- `data/raw/` for original input data
- `data/processed/` for cleaned datasets
- `data/outputs/` for generated reports and artifacts
