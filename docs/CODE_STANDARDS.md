# Code Standards

This document outlines code and data handling standards for P1.

## Conventions

- Use clear naming for datasets and variables
- Keep raw data immutable in `data/raw/`
- Store derived datasets in `data/processed/`
