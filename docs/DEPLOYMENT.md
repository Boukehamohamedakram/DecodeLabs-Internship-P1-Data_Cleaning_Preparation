# Deployment

This document describes how to package, share, and commit the P1 project.

## Deployment Notes

- Ensure raw data is stored in `data/raw/`
- Keep outputs in `data/outputs/`
- Use `config/.env.example` for environment settings
